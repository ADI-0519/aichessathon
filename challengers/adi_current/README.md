# Adi's current, frozen as an opponent

Copied from `origin/adi_branch:current/` so V11 can be measured against the
build the team would otherwise ship: king-bucketed evaluation plus the
quiescence evaluation cache, without this branch's search selectivity.
